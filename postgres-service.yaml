apiVersion: v1
kind: Service
metadata:
  name: postgres-service
spec:
  type: NodePort
  selector:
    app: postgress-depl
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
      nodePort: 30007   # Pick a port between 30000�32767
      # this is additional line to check branch working 
